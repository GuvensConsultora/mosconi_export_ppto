
<odoo>
    <record id="action_payment_transaction_export" model="ir.actions.server">
        <field name="name">Exportar a Excel</field>
        <field name="model_id" ref="payment.model_payment_transaction"/>
        <field name="binding_model_id" ref="payment.model_payment_transaction"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
action = env['payment.transaction.export.wizard'].create({}).action_export()
        </field>
    </record>
</odoo>
